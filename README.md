# clinica (PROJETO JAVA CRUD (cread, read, update e delete))

Para testar o projeto que contém uma interface gráfica, criada utilizando o swing. É necessário rodar o arquivo de script que está no projeto 
